angular
  .module('app')
  .component('user', {
    templateUrl: 'app/user/user.html',
    controller: function () {
      var ctrl = this;

      // General variables
      ctrl.genVars = {

      };

      // Tools

      // Labels
      ctrl.labels = {

      };

      // Labels buttons
      ctrl.btns = {

      };

      // Functions
      ctrl.fns = {

      };
    }
  });
