angular
  .module('app')
  .component('auxs', {
    templateUrl: 'app/auxs/auxs.html',
    controller: function () {
      var ctrl = this;

      // General variables
      ctrl.genVars = {

      };

      // Tools

      // Labels
      ctrl.labels = {

      };

      // Labels buttons
      ctrl.btns = {

      };

      // Functions
      ctrl.fns = {

      };
    }
  });
